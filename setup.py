from setuptools import setup

setup(name='duration-generator',
      version='0.1',
      description='A library of generators that allow looping for durations rather than numbers of iterations',
      author='Ramsey Barghouti',
      license='GPL3',
      packages=['duration'],
      zip_safe=False)
